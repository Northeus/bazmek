[project]
name = "bazmek"
version = "0.0.1"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["bazmek"]

[tool.setuptools.package-dir]
"bazmek" = "src/bazmek"
